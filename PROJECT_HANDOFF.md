# یادداشت انتقال پروژه (برای Claude بعدی)

این فایل را قبل از هر تغییری در پروژه بخوان.

## این تحویل چه چیزی عوض شد؟

۱. **ترفندهای آرایش** (`src/components/makeup/MakeupTipsView.tsx`)
   - از ۱۰ به ۲۰ مطلب رسید (آیتم‌های ۱۱ تا ۲۰ اضافه شدند).
   - عکس آفلاین جدید برای هرکدام: `public/assets/makeup_photos/tip-11-contour.png` تا `tip-20-lash-care.png`.
   - یک نوار فیلتر (جستجو + دسته‌بندی، دقیقاً هم‌ساختار با فیلتر بخش «مقالات») اضافه شد.

۲. **مقالات (پایگاه دانش)** (`src/services/content/trendArticles.ts`)
   - از ۲۲ به ۳۲ مقاله رسید (شناسه‌های `art_trend_23` تا `art_trend_32`).
   - ۵ مقاله جدید به دسته «سلامت مو» (`cat_hair`) اضافه شد (روغن‌درمانی، شامپوی ضدریزش، آسیب حرارتی، کراتین/صافی، کمبود آهن).
   - ۵ مقاله دیگر به دسته‌های روتین، ترکیبات، هورمون، ضدآفتاب و سبک زندگی اضافه شد.
   - عکس آفلاین جدید برای هرکدام: `public/assets/real/articles_new/art_trend_23.jpg` تا `art_trend_32.jpg`.
   - فیلتر بخش مقالات (`src/components/knowledge/KnowledgeCenter.tsx`) از قبل جستجو + تب دسته‌بندی داشت؛ چیزی در آن تغییر نکرد، فقط محتوای جدید از همان مکانیزم عبور می‌کند.

## درباره عکس‌های موقت (placeholder)

همه عکس‌های جدید بالا، **جای‌گیر آفلاین** هستند (نه عکس واقعی محصول/مدل). با یک اسکریپت داخلی (HTML→تصویر، بدون نیاز به اینترنت) ساخته شدند تا:
- متن فارسی رویشان درست و خوانا باشد (نه بهم‌ریخته)،
- هرکدام مشخص کند مال کدام آیتم است (شماره + عنوان روی خودش نوشته شده).

برای جایگزینی با عکس نهایی: فقط فایل PNG/JPG را با همان نام و همان مسیر عوض کن؛ کد نیازی به تغییر ندارد. اگر اسم فایل عوض شد، فیلد `image` (در MakeupTipsView) یا `imageUrl` (در trendArticles) همان آیتم را هم به‌روز کن.

## نکات فنی برای ادامه کار

- این محیط به اینترنت دسترسی نداشت، پس `npm ci` اجرا/تست نشد. قبل از هر build واقعی حتماً `npm ci && npm run lint` را اجرا کن.
- تمام محتوای متنی مقالات و ترفندها، متن اورجینال نوشته‌شده به سبک محتوای موجود پروژه است (نه کپی از یک سایت خاص) — این با قانون «هیچ عدد جعلی» و لحن غیرتشخیصی/غیرنسخه‌ای پروژه هم‌خوان است (نگاه کن به `README.md`، بخش «مرز پزشکی»).
- ساختار داده مقالات تغییر نکرد (`Article` در `src/types.ts`)؛ فقط رکورد جدید اضافه شد.

## یادداشت‌های قبلی (از نسخه‌های قبل پروژه)

برای سابقه کامل پروژه (فروشگاه، بک‌اند حساب کاربری، چرخه ماهانه و غیره)، فایل‌های حافظه Claude با کلید `roza-shop` را ببین — کاربر خواسته این یادداشت‌ها بین نسخه‌ها منتقل شوند.
